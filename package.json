{
  "name": "seatmaps-react-native",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "demo": "npm run start --workspace=apps/demo",
    "build": "npm run build --workspace=packages/react-native-seatmap",
    "test": "npm run test --workspace=packages/react-native-seatmap",
    "typecheck": "npm run typecheck --workspace=packages/react-native-seatmap"
  }
}
